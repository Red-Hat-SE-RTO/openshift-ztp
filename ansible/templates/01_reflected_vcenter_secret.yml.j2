---
apiVersion: v1
kind: Secret
metadata:
  name: {{ vcenter_credential_secret_name }}
  namespace: {{ cluster_name }}
  annotations:
    reflector.v1.k8s.emberstack.com/reflects: "{{ credential_namespace }}/{{ vcenter_credential_secret_name }}"
  labels:
    name: {{ cluster_name }}
    cloud: vSphere
    vendor: OpenShift
    datacenter: {{ vcenter_datacenter }}
    cluster: {{ vcenter_cluster }}
    cluster-name: {{ cluster_name }}
data: